-r base.txt

pytest==9.1.1
pytest-django==4.13.0
ruff==0.16.2
black==26.5.1
# Capped by django-stubs' compatible-mypy extra: mypy<1.20,>=1.13
# as of django-stubs 5.2.9. mypy 2.x is NOT yet supported.
mypy==1.19.1
# 5.2.x line matches our pinned Django 5.2 LTS (full support, vs. only
# partial 5.2 support from the 6.0.x line built for Django 6.0).
django-stubs[compatible-mypy]==5.2.9
