Django==5.2.17
psycopg[binary]==3.3.4
# [redis] extra pulls kombu's redis transport, which caps the redis-py
# version below (see the explicit pin) — see docs/DECISIONS.md.
celery[redis]==5.6.3
# Capped by kombu (celery's transport lib): redis!=4.5.5,!=5.0.2,<6.5,>=4.5.2
# as of celery 5.6.3. redis-py 7.x/8.x are NOT yet supported by kombu.
redis==6.4.0
django-environ==0.14.0
